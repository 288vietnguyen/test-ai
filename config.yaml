aws:
  profile: default        # AWS SSO profile name (run: aws sso login --profile <name>)
  region: us-east-1

model:
  id: anthropic.claude-3-5-sonnet-20241022-v2:0
  max_tokens: 4096
  temperature: 0.7

system_prompt: "You are a helpful AI assistant. Be concise and accurate."
